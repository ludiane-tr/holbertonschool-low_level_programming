#include <stdio.h>

/**
 * main - Prints the program name, followed by a new line.
 * @argc: parameters
 * @argv: parameters
 *
 * Return: Always o.
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
