#include "main.h"
#include <stdio.h>
#include <math.h>
/**
*int_abs - write a function that computes the absolute value.
*@n: parameter.
*/
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
