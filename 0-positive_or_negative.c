#include <stdio.h>
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main (void)
{
	int n;
	if (n > 0)
		printf("%d is positive\n", n);
	if (n == 0)
		printf("%d is zero\n", n);
	if (n < 0)
		printf("%d is negative\n", n);

	return(0);
}
